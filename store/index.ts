// const myPlugin = store =>
//   store.subscribe((mutation, state) => {
//     if (mutation.type === 'soundcloud/setTrackNumber') {
//       const currentlyPlaying = state.soundcloud.tracks.filter(x => x.playing)
//       // console.log(currentlyPlaying)
//       // console.log('tracknumber', state.soundcloud.trackNumber)
//       // console.log(mutation.payload)
//     }
//   })

// export const plugins = [myPlugin]
